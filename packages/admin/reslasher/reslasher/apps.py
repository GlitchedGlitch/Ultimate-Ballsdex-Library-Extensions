from django.apps import AppConfig

class ReSlasherConfig(AppConfig):
    name = "reslasher"
    verbose_name = "ReSlasher"
    default_auto_field = "django.db.models.BigAutoField"
    dpy_package = "reslasher.reslasher"
